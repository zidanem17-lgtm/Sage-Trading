import React from "react";
import ReactDOM from "react-dom/client";
import SageTrading from "./SageTrading.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SageTrading />
  </React.StrictMode>
);
